package main

import (
	"fmt"
	"sort"
)

func main() {
	fmt.Println(solution([]int{10, 2, 5, 1, 4, 20}))
}

func solution(someArray []int) int {
	//sort the array first
	sort.Ints(someArray)
	strin

	for index := 0; index < len(someArray); index++ {

	}

	//if no triangle is found
	return 0
}
